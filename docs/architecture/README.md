# 아키텍처 (맵)

## 목표

- 에이전트/사람 모두가 **예측 가능한 구조**에서 빠르게 변경할 수 있게 한다.
- 문서만으로 일관성이 유지되지 않으면 **린트/테스트/CI로 강제**한다.

## 현재 적용 규칙

- **FSD 레이어/의존성**: `.cursor/rules/fsd-core.mdc`, 슬라이스 템플릿·scaffold는 `.cursor/skills/fsd-slices/SKILL.md`를 기준으로 한다.

## 이 문서에 추가할 내용(필요 시)

- 주요 엔트리 포인트(앱 부팅/라우팅/상태 관리)
- 도메인 경계(entities/features/widgets/pages 분리 기준)
- “허용되는 의존성”과 “금지되는 의존성”의 대표 예시
