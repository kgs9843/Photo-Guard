---
alwaysApply: true
---

## 프로젝트 불변 규칙(최우선)

- **항상 한국어로 답변**합니다.
- 프로젝트에서 **에러 처리는 try-catch로 구현**합니다. (Promise 체인만으로 흐름을 숨기지 않기)
- **해당 프로젝트의 eslint 규칙을 준수**합니다. (임의로 규칙 완화/비활성화 금지)
- UI는 **`src/index.css`의 `@theme` 토큰을 사용**합니다.

<!-- ## 에이전트 운영 원칙(하네스 엔지니어링 반영)

- 긴 만능 지침 파일 1개에 모든 규칙을 몰아넣지 않습니다. (규칙은 짧고, 상세는 `docs/`로 링크)
- “문서/계획/아키텍처”는 레포 안에 남깁니다. 외부 대화/문서에만 있는 지식은 없는 것으로 취급합니다.
- 문서로 충분하지 않으면 **가드레일을 코드(린트/테스트/CI)**로 승격합니다. -->
